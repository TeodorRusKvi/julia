server_1
server_2

interarrival = rng(dist)
iteration = interarrival

while iteration < 50000
    servicetime = rng(dist2)
    if iteration == next custtime
        nextcustime = iteration + rng(dist)
            iteration == busyuntil_1
    
    else iteration == busyuntil_1
        if queue > 0
            queue - 1
            busyuntil = iteration
        end
    end
end
